import React, { Component } from 'react';



class App extends Component {
    // this will need to have state (at least for now -- maybe later only lower), so give this a constructor method and then also do setState above.
    render() {
        return (
            <div>
                Hello from the div in the App component!
            </div>
        )
    }
}

export default App;